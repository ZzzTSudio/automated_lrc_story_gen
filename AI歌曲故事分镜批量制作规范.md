# AI歌曲故事分镜批量制作规范

版本：v1.1｜适用工程：`aimusic`｜交付方式：素材目录与文件链接，不生成ZIP。

本文将M401试作流程整理为可重复执行的批量前期制作规范：**核对输入 → 独立扩写故事 → 锁定时长与分镜 → 独立剧情审查门 → 建立角色和场景参考 → 样片验收 → 逐镜首帧 → 单段提示词合集 → 双重验收 → 目录交付**。

本文件是工作规范，不是已经实现的批处理程序。状态表、重试次数和恢复规则需由执行者或后续调度程序落实；编写本规范不代表开始处理任何歌曲。

## 1. 范围与不可变要求

本流程制作的是可供图生视频使用的**视觉前期素材**，包括详细故事、分场剧本、镜头设计、人物与场景参考、逐镜首帧、最终视频提示词和验收记录。默认不生成视频片段、口型、配音、音乐或最终剪辑，不把静态图片数量及设计片长当作成片完成证据。

必须保持：

- 每首独立构思，不用上一首的人物、对白、剧情转折或分镜骨架替换几个词来生产下一首。目录、时间码和导出格式可以复用，创作内容不能机械套用。
- 不修改原故事文件、`歌词.txt`、现有WAV、原歌曲目录名及编号；新增内容写入指定的分镜目录。
- 每首设计片长控制在240—360秒；未另行指定时，以300秒为默认目标。开工前逐首锁定准确秒数。
- 真人题材以真实场地中的真人电影单帧观感为目标；明确指定动画等其他风格时按用户要求执行。
- 最终每镜只有一段可独立复制的四字段提示词，集中输出，不让使用者手工拼接。
- 默认直接交付文件夹及文件链接，不创建、更新或推荐ZIP等压缩包。“完整素材包”指内容齐全的目录。
- 一首或一张失败不自动重跑整批；已成功且仍有效的内容保留。不得为完成数量而放宽真实感、连续性或技术验收。

## 2. 开工配置与技能版本

工程内技能入口为 [skills/ai-director-storyboard/SKILL.md](./skills/ai-director-storyboard/SKILL.md)。执行前按该入口加载相关参考文件；涉及生图时使用当前可用的 `imagegen` 技能。

如果同时存在工程内副本与个人安装版本，应明确本批采用的实际技能路径，并记录入口、视觉规范和导出/校验脚本的SHA256。不混用两个不同版本；不在批次中静默切换规则。用户的新要求优先，变更后按第11节处理受影响内容，不自动改写已交付结果。

| 配置 | 默认或要求 |
|---|---|
| 输入根目录 | 本工程 `aimusic/outputs`；在本文所在目录执行时为 `./outputs` |
| 处理范围 | 明确的歌曲编号列表或编号区间；未指定范围时不默认处理全部2000首 |
| 单首片长 | 默认300秒，可逐首覆盖为240—360秒 |
| 画幅 | 默认16:9；首帧目标像素尺寸需在首次样片阶段确认并记录 |
| 常规单镜 | 5—10秒；低于5秒须解释剪辑理由；默认不得超过10秒 |
| 镜头数量 | 按剧情与时长计算，不固定为36镜；300秒且每镜5—10秒时，理论范围为30—60镜 |
| 风格 | 真人题材默认自然实拍质感，避免CG、棚拍摆姿和美颜处理 |
| 同时处理歌曲数 | 默认1首；需要并发时显式配置独立输出目录，每个目录仅一个写入者 |
| 同时生图请求数 | 默认最多2个独立资产；这是本批调度默认值，不是平台限额声明 |
| 单资产生成尝试 | 默认最多3次调用，包含首次与后续重试/修订；需增加时另行记录原因及额度 |
| 剧情审查门 | 每首在故事扩写和镜头锁定后、角色/场景参考前，必须由未参与该首创作的审查者书面通过 |
| 样片确认 | 默认由执行者逐项检查并记录；用户明确要求先确认样片时，确认前不得扩展生图 |
| 本地尺寸处理 | 记录是否已明确允许及允许的处理方式；未获所需授权时不擅自转换处理工具 |
| 输出覆盖 | 默认跳过有效完成项、续做有效未完成项；改版重做使用新版本目录 |
| 压缩交付 | 默认关闭 |

只请求写规范、改技能或做一张对照时，按该最小范围执行，不触发批量生产。

## 3. 输入识别与隔离

### 3.1 歌曲定位

只枚举明确输入根目录下的歌曲文件夹，按编号的数值排序，不按字符串排序。编号至少三位并允许继续增长，例如 `M001`、`M401`、`M1000`、`M2000`；保留原编号字符串，不截断、不重新编号。

歌曲文件夹通常为 `M编号-歌名`，但逻辑歌名以故事文件的标题为准。目录中若有人工标记或与标题不同，不自动改名；在输入记录中同时保留原目录名和故事标题。读取路径必须来自实际枚举结果，不靠歌名猜路径。

每首应找到唯一的 `M编号-故事内容.md`。发现缺失、多个候选、编号冲突或重复编号时，标为 `blocked_input` 并说明原因；不随便选一个继续写。

不递归吸收其他根目录中的同名内容。例如 `aimusic/aimusic/outputs` 不因存在相同编号就自动并入 `aimusic/outputs` 的本批清单。

### 3.2 原文范围

每首读取其标题、编号、主题、风格，以及 `## 故事内容` 下的故事正文。正文在下一个同级或更高级标题、独立歌词段落标记（如 `[Verse]`、`[Chorus]`）或文件结尾处结束。原文件可能附有其他歌词稿，不能把整份文件当作同一份故事。

提取边界无法确定时保留候选原文并标记问题，不把附加歌词、版本备注或示例台词误写为原故事事实。

本阶段不读取其他歌曲的创作正文来充当参考。原歌词和音频仅在用户明确要求关联时使用；未试听的WAV不标为“已选曲”“已对齐”。

### 3.3 输入登记

记录 `song_id`、`title`、实际源路径、原文SHA256、正文提取结果、技能版本指纹、批次配置和输出目录。原文及配置发生变化后，旧产物不能仅因文件存在就直接复用。

## 4. 输出目录与版本

首次制作写入对应歌曲目录下的 `storyboard_book/`：

```text
aimusic/
├─ batch_runs/
│  └─ <batch-id>/
│     ├─ batch-manifest.json          选定歌曲、配置、输出目录及状态
│     └─ batch-report.md              本批完成项、待处理项和原因
└─ outputs/
   └─ M编号-原目录名/
      ├─ M编号-故事内容.md             原始文件，只读
      ├─ 歌词.txt                      原始文件，只读
      └─ storyboard_book/
         ├─ story-expanded.md          扩写故事、分场剧本、对白和节拍时间
         ├─ continuity-bible.md        身份、空间、光线、道具状态及真实感设定
         ├─ storyboard-manifest.json   镜头与资产的结构化清单
         ├─ storyboard-book.md         逐镜时间码、画面、声音、首帧及最终提示词
         ├─ video-prompts.txt          按镜号集中，每个非空行一段最终提示词
         ├─ validation-report.md       技术检查、视觉检查、未解决问题
         ├─ storyboard-gallery.html    可选：离线图文浏览页
         ├─ characters/                已验收角色参考图
         ├─ scenes/                    已验收场景锚点
         ├─ frames/
         │  ├─ S01.png
         │  └─ ...                     按连续镜号保存已选定首帧
         └─ production/
            ├─ input-record.json       输入与技能指纹
            ├─ asset-log.jsonl         各资产尝试、问题、选择与验收记录
            ├─ asset-audit.json        图片格式、尺寸、字节数和SHA256
            └─ attempts/               未验收候选及修订历史
```

Markdown、HTML及清单内的图片引用使用相对于分镜目录的路径。最终有效资产必须在工程内，不能仅引用生成工具的默认保存目录。最终回复提供绝对路径的可点击链接。

已有完成目录且输入及配置未变时，验证后跳过；已有未完成目录且输入仍匹配时断点续做。原文、角色或视觉规范改变，或用户要求重做时，默认新建 `storyboard_book_v002/` 等版本目录，并在批次清单中登记。不得覆盖旧版已验收内容来假装续做。

`attempts/` 用歌曲内的资产ID和尝试号命名，例如 `S08/attempt-002.png`。候选验收通过后才成为 `frames/S08.png` 的选定版本；未被选中的文件不混入正式镜号计数。保留必要历史，不为本规范额外生成压缩包。

## 5. 单首执行步骤与放行条件

| 阶段 | 必做工作 | 产物 | 进入下一阶段的条件 |
|---|---|---|---|
| A 输入核对 | 提取当前故事、核对编号和范围、登记指纹 | 输入记录 | 原文唯一、可解析，无关键冲突 |
| B 故事扩写 | 明确欲望、阻力、信息变化和结尾选择；写场景、动作、对白 | 详细剧本 | 保留核心人物关系、冲突与结局；新增内容可识别 |
| C 镜头锁定 | 分配节拍和逐镜时长、声音、画面状态 | 分镜清单 | 时长精确相加，镜号连续，对白可正常表演 |
| C.5 剧情审查门 | 独立审查故事与镜头节拍，评估逻辑、节奏、人物行动和情绪发展 | `narrative-review.md` | 得到 `pass_to_visual`；未通过则返回B/C修订 |
| D 连续性设定 | 锁定人物、服装、空间、光源、轴线、道具状态 | 连续性文档 | 各镜能明确引用，不靠临时重新设计 |
| E 基础参考 | 生成并检查角色与场景图 | 角色/场景资产 | 身份、材质、空间及实拍观感通过 |
| F 样片 | 从真实镜表选少量代表镜头生成 | 样片及检查记录 | 真实感、构图、连续性、道具状态可接受 |
| G 完整首帧 | 依赖已验收参考，生成剩余镜头并逐张查看 | 全部首帧 | 无缺镜，关键人物、道具、状态无错误 |
| H 提示词与导出 | 按实际首帧回填位置，合并最终提示词，输出分镜书 | 提示词合集与分镜书 | 每镜一段，内容自洽，链接有效 |
| I 最终验收 | 分别执行技术检查和视觉检查，更新本批台账 | 验收报告 | 两类检查均通过才标记完成 |

同一首必须遵守依赖顺序：角色/场景参考未验收，不能先把所有首帧提交出去；剧情审查未通过，不能生成角色或场景参考；样片未通过，不能用“大批生成后再挑”代替修正基础问题。

批量启动时先让一首代表歌曲走通样片阶段，确认当前工具、保存方式和视觉方向可用，再扩大同类处理；之后每首仍需自己的参考与样片检查。检查默认是执行者的工作，不自动变成要求用户逐镜批准。

### 5.1 剧情审查门

主导创作者完成B、C阶段后，提交当前歌曲的原故事正文、`story-expanded.md`、节拍表和尚未生成图片的镜头草案。审查者只依据这一首的材料工作，不读取其他歌曲的剧情正文作为创作参照，不直接重写主导创作者的故事，也不提前建立角色、场景或首帧。

审查必须评估：主角目标、阻力、关键选择与结尾是否清楚；因果和转折是否有铺垫；人物关系、动机和结局是否忠于原故事；情绪是否有推进而非重复；每个节拍是否在改变行动、关系或观众认知；对白是否能以正常语速落入镜头；以及镜头草案是否能用具体动作、空间、位置和道具表达故事，而不是依赖旁白或抽象解释。

审查报告保存为 `storyboard_book/narrative-review.md`，至少写明：审查范围、结论、已成立内容、按优先级排序的问题、可执行的修改方向、需要复核的镜头/节拍和审查者身份。结论只能是：

- `pass_to_visual`：主导创作者可进入D阶段，建立连续性、角色和场景参考。
- `revise_narrative`：主导创作者只修订B/C阶段；修订后由同一审查角色或指定替补重新审查，仍不可进入D阶段。
- `needs_human_decision`：原故事存在无法可靠推断的关键冲突或用户偏好缺失；保留现有材料并停止该首的视觉阶段。

每次返工在报告中追加轮次和问题处理结果，不覆盖旧意见。默认最多两轮“审查—修订—复审”；第二轮仍有关键问题时标为 `needs_review`，不以赶进度为由放行。审查通过后，主导创作者在续做记录中引用报告路径和通过轮次，再进入D阶段。

### 5.2 本批M402—M410角色分配

本批共有9首歌曲、6名主导创作者和3名独立剧情审查者。主导创作者按下表仅处理分配给自己的歌曲；审查者不审自己对应主导的歌曲。由于运行槽位有限，角色可分波启动，但角色身份和审查独立性不变。

| 主导创作者 | 负责歌曲 | 对应独立审查者 |
|---|---|---|
| 主导1 | M402、M408 | 审查3 |
| 主导2 | M403、M409 | 审查1 |
| 主导3 | M404、M410 | 审查2 |
| 主导4 | M405 | 审查3 |
| 主导5 | M406 | 审查1 |
| 主导6 | M407 | 审查2 |

审查1负责M403、M406、M409；审查2负责M404、M407、M410；审查3负责M402、M405、M408。审查角色只在收到对应歌曲的B/C阶段产物后启动；通过结论回传给对应主导创作者，返工也只回传给该主导创作者。

## 6. 故事扩写与时长规则

每首先写清四件事：谁想得到什么、什么具体事实阻止了他/她、哪条新信息改变局面、结尾主角做了什么选择。随后再扩写人物动作与对白，不能先套镜头模板。

允许补充原故事未写细的场景、对白、动作及时间衔接；不得擅自改变人物关系、关键动机或结局。拿不准的关键事实写入待确认项，不把推测记成原文。原作、改编补充和制作假设在 `story-expanded.md` 中区分。

每个节拍记录目标、阻力、信息变化、情绪走向、视觉意象和时间范围。每镜只承担一个主要叙事动作与一个主要摄影意图；重复动作可省略，不为凑300秒延长无信息画面。

时长验算必须满足：

```text
240 ≤ 单首锁定总秒数 ≤ 360
Σ 每镜 duration = project.total_duration_seconds
相邻时间码首尾衔接，最后一镜结束时间 = 锁定总秒数
```

动作与线索常用5—6秒，短对白与反应常用7—8秒，空间建立和完整表演可用9—10秒。长对白用跨镜声音延续或拆镜，不让演员高速念词。片名、黑场或片尾若计入设计片长，必须显式包含在镜表时长中，不在导出后再追加。

M401的“六段、36镜、300秒”只是一首的处理方案，其他歌曲不需要复制其段数、镜数或感情故事结构。

## 7. 真实感、角色与场景规范

### 7.1 目标与判断

目标是“真人在可信物理空间中被电影摄影机拍到的一瞬”。高清、尺寸正确或脸部好看都不足以证明真实。M401及S08对照只能作为流程与问题案例；S08的新规范单次重绘仍有合成纹理，不能将其标为已经证明真实感改善的合格基准。

| 检查项 | 提示词需要说明 | 退回修订的典型问题 |
|---|---|---|
| 皮肤与头发 | 当前景别能看到的正常肤色变化、细腻肤质、局部反光、自然发丝 | 蜡像、塑料、均匀湿亮、网格状/雕刻状假纹理 |
| 衣料与身体 | 合理厚薄、受力褶皱、重力垂坠、自然重心 | 浮在身上的衣服、贴图式织纹、僵硬展示姿势 |
| 光源与曝光 | 实际窗/灯的位置、方向、软硬及暗部，合理的反射补光 | 无来源轮廓光、所有脸均匀补亮、HDR边缘、强行青橙调色 |
| 摄影与景深 | 景别、机位高度、镜头方向、焦点和空间距离相符 | 抠图虚化、失焦断层、透视不合理、全画面过锐 |
| 空间与道具 | 功能可信、尺度合理、接触阴影与遮挡正确 | 实验设备变成家电、道具悬浮、样板间感、反射不符 |
| 表演与构图 | 目光对象明确，动作前姿态自然，站位服务关系 | 所有人盯镜头、并排摆拍、复制面孔与表情 |

不靠加噪点、强行压暗、故意变脏或增加痘痘来制造真实感；不把“肤质自然”写成“夸张毛孔”。只描述当前景别真正能看见且符合剧情的细节。

### 7.2 参考资产与继承范围

核心角色先建立固定身份、发型、服装及道具持有关系。角色设定图按技能规定制作，观感应接近真人选角/服装记录照；本身存在塑料感时先修参考。

角色参考仅用于身份、体型、发型和服装，不继承灰色背景、均匀棚拍光、展示站姿、表情和多视图布局。场景参考仅继承固定空间和材质，不覆盖本镜的人物站位、时间、可移动道具或动作状态。

反复出现的空间先建立锚点，记录门窗、家具、光源、拍摄方向。一次性简单外景可用明确文字设定。不要把别的场景道具写进通用参考说明，也不要每首复用同一个主角面孔，除非用户要求系列固定人物。

### 7.3 样片放行

每首通常选三张正式镜表中的首帧：人物近景、双人关系镜头，以及本首最难的低光/多人/反光/手部道具镜头。若本首没有相应人物或镜头类型，选能覆盖其真实风险的镜头，不为了凑测试类型编造剧情。

样片通过后直接计入正式镜头，不另生一套重复图片。若没有通过，记录具体问题并修订参考、光源、构图或道具描述，不仅追加“更真实”。达到尝试上限仍无法接受时，暂停该首生图并进入 `needs_review`；已经完成的文稿保留。

## 8. 首帧与最终提示词

### 8.1 首帧生图提示

`generation_prompt` 保存实际发送的首帧提示，至少说明：各参考图用途、场景时段、可见人物与位置、静态姿态、道具当前状态、材质、光源、机位、焦点和约束。使用 [视觉规范](./skills/ai-director-storyboard/references/visual-pipeline.md) 的实拍描述原则。

首帧停在动作开始前：折信前纸完整，抽信前信仍在本子里，关灯前灯亮，拉帘前帘未拉开。静态不等于摆拍；手、身体重心和视线仍要符合即将发生的动作。

每个独立资产单独调用生成工具，不用一张多格拼贴代替多个镜头。多视图角色设定是一份参考资产，不等于多个剧情首帧。精确纸面文字、手机界面与字幕默认留后期，另列内容、位置和持续时间。

### 8.2 最终视频提示词

每镜以同一对半角方括号包住以下四个字段，字段顺序固定，正文不换行：

```text
[场景：当前地点、时间、可见环境和有来源的光线。角色：角色名（图中实际位置的人）、其他出镜角色，必要的身份和服装约束。镜头：景别，焦段，景深，对焦与机位或必要运镜，真人实拍质感要求。动作：本镜数秒内的一个主要动作与表情变化；对白直接写为某人对某人说“原话”，必要的物理稳定约束也合入这里。]
```

例如：

```text
[场景：夜晚的大学实验记录室，冷白顶灯照亮白色桌面，人物背光侧保留自然暗部。角色：林澄（图中左侧的人）、周屿（图中右侧的人），保持首帧的身份、自然肤质和服装。镜头：双人中景，40mm镜头，中等景深，眼平固定机位，成像具有真人现场摄影的自然层次。动作：林澄把记录本沿桌面缓慢推向周屿，对他说“都核过了。一路顺风”，周屿安静地看着她；手指与本子接触真实，本子不悬浮，两人不看摄影机。]
```

格式和内容要求：

- `场景：`、`角色：`、`镜头：`、`动作：` 各出现一次，不额外加“风格”“负面提示”“对白”等独立字段。
- 单人、无人、手部、背影及画外说话者如实区分；角色左右位置按**当前首帧**填写，不按角色设定图位置填写。
- 生成图与原计划略有差异时，先判断是否破坏叙事或连续性。仅景别等可接受变化可回填；人物出错、道具状态错、轴线错等必须修图，不能修改说明掩盖错误。
- 无对白时写“无对白”；有对白时必须与剧本和时长一致。不得把后期字幕当成演员说出的台词。
- 首帧提示不替代最终视频提示，最终视频提示也不要求使用者再粘贴其他提示块。

`video-prompts.txt` 按清单镜号顺序集中保存，**每个非空行对应一镜**，段落之间可留一个空行；不加编号、标题、时间码或解释。镜号与时间码的对应关系放在分镜书中。导出后与 `shots[].video_prompt` 逐条核对，不能遗漏或错位。

## 9. 批量台账、失败与重试

批次总清单至少记录每首的ID、源文件、输入指纹、输出目录、目标时长、当前状态、当前阶段、已验收镜数、待处理资产、问题原因和最后更新时间。`queued` 不代表已生成；“有文件”不代表已验收。

| 状态 | 含义及处理 |
|---|---|
| `queued` | 已明确选入本批，尚未开始 |
| `running` | 正在某个阶段执行，另记阶段名 |
| `needs_review` | 创作/视觉问题达到修订上限，或关键选择无法确定；保留成果并列出问题 |
| `blocked_input` | 源文件缺失、冲突或不可解析 |
| `blocked_service` | 登录、额度、权限或服务条件阻止继续，禁止绕过限制 |
| `pending_dimensions` | 内容已产出，但严格尺寸要求尚未满足 |
| `pending_verification` | 所需验收尚未完成，不能记为交付完成 |
| `complete` | 本首范围内必需产物齐全，技术与视觉验收均通过 |
| `skipped_unchanged` | 输入和配置匹配，已有完成结果重新核对有效，本批未重生 |

一次资产尝试记录：歌曲ID、资产ID、尝试号、用途（首次/修订/重试）、参考文件及指纹、实际提示词、可获得的请求或任务ID、返回路径、工程内候选路径、结果、具体缺陷与选择理由。没有平台任务ID时如实留空，不伪造。

请求超时或返回不确定时，先检查已有返回记录及已知输出路径/任务状态，确认是否已产出，再决定重试，避免重复生成或重复计费。网络恢复不能改变画风、模型或参考图而不留记录。

每个资产默认最多3次生成调用；每次修订对应可见问题，不能无休止重抽。达到上限仅挂起受影响资产或歌曲，继续无依赖的其他工作。若同一登录、额度或权限问题影响整批，停止提交新请求并报告，不对每首重复撞同一个错误。

候选先保存、核对、验收，再更新清单与选定文件。更新JSON时避免留下半写文件；多个执行者不能同时改一首的清单或同一个最终文件。

## 10. 技术与视觉双重验收

### 10.1 技术检查

- 源ID、清单ID和输出目录对应；不存在跨歌曲错写。
- 镜号从S01连续递增，无重复、缺号或候选混入；时间码连续，总秒数等于锁定片长且在240—360秒范围内。
- 全部必需图片实际存在、非空、可解码；核对角色、场景、首帧数量，不只检查 `frames`。
- 读取文件实际像素尺寸与比例，不以提示词里写了分辨率就当作已经达标。
- 同一首的正式首帧尺寸一致，并符合本批锁定规格；若批次约定各首共用规格，还需跨首检查。
- 全部图片引用存在且可随目录移动；每条提示词、对白、实际人物位置与对应镜号一致。
- 台账中的完成数以验收记录为准，而不是调用次数、PNG文件总数或文件夹数量。

M401曾出现1670/1671/1672×941的宽度差异。此类1—2像素问题不能伪报为“尺寸一致”，也不应无限重生已合格的画面。若当前工具规则要求本地尺寸处理获明确授权，应在批次配置中记录已获授权的范围与方法，后续同范围内不重复询问；未获授权则保留原图、标记 `pending_dimensions`，继续不受影响的文稿及其他歌曲。

尺寸处理获允许后，保留原始图，只执行约定的尺寸或画布处理，不借机改人物、材质或构图，不无说明拉伸人物或裁掉关键内容；处理后重新读取尺寸、复看画面、更新引用和审计。尺寸问题不能成为弱化校验器的理由。

### 10.2 视觉检查

逐张检查出镜人物、身份、服装、空间、镜头方向和首帧动作状态。重点复看开场、反转、手部道具、复杂多人、低光以及结尾；与相邻镜头对照检查道具交接和时间变化。

真实感与连续性分别记录 `pass / fail / pending` 及理由。有塑料脸、异常皮肤/衣料纹理、复制人物、关键道具变形、完整纸变纸船、灯提前熄灭等问题时不能只因“图很清晰”而通过。

修改某张图后，复核相邻镜头与最终提示词，不机械重跑整片。脚本无法代替视觉判断。

### 10.3 现有脚本的使用边界

在 `aimusic` 目录下，以已经确认存在的单首输出目录执行以下命令。示例以M401路径演示命令形式，不是要求重跑M401：

```powershell
$skillRoot = (Resolve-Path -LiteralPath '.\skills\ai-director-storyboard').Path
$bookRoot = (Resolve-Path -LiteralPath '.\outputs\M401-窗帘后面的灯\storyboard_book').Path
$manifestPath = Join-Path $bookRoot 'storyboard-manifest.json'

python -X utf8 (Join-Path $skillRoot 'scripts\validate_storyboard.py') $manifestPath --root $bookRoot
python -X utf8 (Join-Path $skillRoot 'scripts\build_storyboard_book.py') $manifestPath --output (Join-Path $bookRoot 'storyboard-book.md') --prompts-output (Join-Path $bookRoot 'video-prompts.txt')
```

预检查可使用校验脚本的 `--skip-images`，但其结果只代表跳过图片的检查，不能作为完整视觉交付通过证据。导出成功同样不代表图片已通过。

当前脚本覆盖镜号、时长、部分清单与单段格式检查，以及逐镜图片存在性、PNG头部尺寸和比例；并未实现本文的整批调度、完整图像解码、跨歌曲防串写、所有参考资产检查或真实感判断。这些须另行检查并写入报告，不能因一次 `PASS` 就声称全部自动验证。

如果制作可选HTML页，检查链接、图片显示、筛选和复制。浏览器安全策略限制某种打开方式时，不绕过限制；保留可用的Markdown/文本交付并注明未完成的交互检查。HTML是可选项时，该限制不妨碍其他已验证必需产物交付；若用户明确要求可用且已验收的网页，则网页验收仍是未完成项。

## 11. 断点恢复与改版

恢复时先核对原文、配置、技能及依赖资产的指纹，再检查磁盘上的实际文件与日志。状态显示 `running` 但请求状态未知时，先消除请求不确定性，不直接重新提交。

| 变化 | 需要重新检查的范围 |
|---|---|
| 仅导出样式或提示词分隔格式变化 | 重导出对应文本并校验；不默认重生图片 |
| 某镜对白或时长变化 | 该镜、相邻声音/剪辑衔接及全片总时长 |
| 某镜构图或道具状态变化 | 该镜图片、最终提示词、相邻连续性 |
| 角色身份/服装参考变化 | 所有引用该角色的镜头 |
| 场景结构或光线基准变化 | 所有引用该场景且受影响的镜头 |
| 原故事核心事件或结局变化 | 从故事扩写重新审核，检查其后全部依赖 |
| 实拍视觉规范变化 | 先做新规范样片；不把旧图自动标为新规范合格 |

同一问题已在一镜修好时，可以复用修订经验，但不能不看图就批量认定所有镜头已修复。改版产物使用新目录或明确的版本记录，旧版不被默默替换。

## 12. 批次清单示例

下面是未来批次的配置示例，不代表这两首已开始执行。路径相对于 `aimusic`；真正执行时应填入实际输入指纹和技能指纹。

```json
{
  "schema_version": "1.0",
  "batch_id": "pilot-001",
  "status": "draft",
  "input_root": "outputs",
  "skill_root": "skills/ai-director-storyboard",
  "defaults": {
    "target_duration_seconds": 300,
    "aspect_ratio": "16:9",
    "target_frame_size": null,
    "preferred_min_shot_seconds": 5,
    "max_shot_seconds": 10,
    "max_active_songs": 1,
    "max_inflight_image_requests": 2,
    "max_attempts_per_asset": 3,
    "allow_local_dimension_normalization": false,
    "archive_output": false
  },
  "songs": [
    {
      "song_id": "M402",
      "source_path": "outputs/M402-没发出的语音/M402-故事内容.md",
      "output_dir": "outputs/M402-没发出的语音/storyboard_book",
      "status": "queued",
      "phase": "input",
      "source_sha256": null,
      "planned_shots": null,
      "accepted_shots": 0,
      "issues": []
    },
    {
      "song_id": "M403",
      "source_path": "outputs/M403-雨停在你站台/M403-故事内容.md",
      "output_dir": "outputs/M403-雨停在你站台/storyboard_book",
      "status": "queued",
      "phase": "input",
      "source_sha256": null,
      "planned_shots": null,
      "accepted_shots": 0,
      "issues": []
    }
  ]
}
```

`target_frame_size: null` 仅允许出现在准备/样片阶段；扩展生成剩余首帧前必须根据本批工具与交付要求锁定目标规格。空指纹也必须在进入创作前补齐。不能把示例当成已有任务记录。

每首分镜清单沿用 [manifest-schema.md](./skills/ai-director-storyboard/references/manifest-schema.md)，并保留 `character_placement`、`reference_images`、`generation_prompt`、`video_prompt` 和必要的修订记录。未知生成参数如随机种子不得编造。

## 13. 可直接复用的批量执行指令

复制以下文本时，先填入明确编号列表或范围。默认只执行所选歌曲的前期制作，不生成视频、不压缩交付：

```text
请依据《AI歌曲故事分镜批量制作规范.md》和本批指定版本的 ai-director-storyboard 技能，处理歌曲【填写明确编号列表或范围】，不要扩展到其他歌曲。

每首只使用自己的“故事内容”正文作为原作依据，不混入同文件附加歌词，不复用其他歌曲的剧情、对白或人物。保留原故事、歌词、音频与目录名，新增输出写入对应歌曲的分镜目录；已有有效成果按输入指纹与验收记录跳过或续做，重做另建版本目录。

默认每首锁定300秒、16:9，常规单镜5—10秒，总时长须精确相加。逐首完成详细故事、分场剧本、节拍、连续性设定、角色与场景参考、少量样片验收，再生成剩余逐镜首帧。镜数与段数按剧情设计，不套用M401的36镜或六段结构。

真人画面追求真实现场摄影观感：材质、光源、透视、景深和表演必须可信，不能用美颜、加噪点或“8K、超写实”等词替代判断。样片未达到标准时先修参考或提示，不继续放大错误。每个资产默认最多3次生成尝试，失败保留日志并挂起受影响项，不重做成功项。

最终提示词按实际首帧位置写成一段：[场景：……。角色：……。镜头：……。动作：……。]，对白合入动作。所有镜头集中写入video-prompts.txt，每个非空行一镜；镜号、时间码和制作备注放进分镜书。

核对全部文件、真实尺寸、镜号、时长、提示词、引用与视觉连续性，分别报告技术和视觉结果。尺寸或工具权限问题如实记录，遵循已获授权范围，不绕过限制。以素材目录、分镜书、提示词合集、参考与首帧、验收报告直接交付，不生成ZIP。

维护本批清单与进度报告，区分已完成、未完成、失败、待复核和未改动跳过项。不要把设计片长、静态首帧或文件存在性说成视频成片已经完成。
```

## 14. 完成汇报

每首提供素材目录、详细故事、分镜书、提示词合集、清单与验收报告的文件链接，并报告准确设计片长、镜头数量、实际首帧规格、修订次数及未解决问题。可选HTML或总览图只有实际生成后才提供。

批次报告列出：本批选择数量、通过数量、未改动跳过数量、待处理数量、各首输出目录、当前阶段和具体原因。所有选中歌曲都达到 `complete` 或有效的 `skipped_unchanged`，才能说本批全部完成；存在 `needs_review` 或任何待处理状态时，明确说明已交付范围及剩余工作。

交付时不创建ZIP，不把“等待打包”作为未完成原因，也不把可选的网页或总览图强行变成每首的额外生产任务。
